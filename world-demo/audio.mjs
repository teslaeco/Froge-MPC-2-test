export function makeAudio(kind='beep'){if(!['beep','alarm','engine'].includes(kind))throw Error('Nieobsługiwany dźwięk.');const rate=44100,seconds=kind==='beep'?1:4,count=rate*seconds,buffer=new ArrayBuffer(44+count*2),v=new DataView(buffer);const str=(at,s)=>[...s].forEach((c,i)=>v.setUint8(at+i,c.charCodeAt(0)));str(0,'RIFF');v.setUint32(4,36+count*2,true);str(8,'WAVE');str(12,'fmt ');v.setUint32(16,16,true);v.setUint16(20,1,true);v.setUint16(22,1,true);v.setUint32(24,rate,true);v.setUint32(28,rate*2,true);v.setUint16(32,2,true);v.setUint16(34,16,true);str(36,'data');v.setUint32(40,count*2,true);
for(let i=0;i<count;i++){const t=i/rate,fade=Math.min(1,t*15,(seconds-t)*15);let n=kind==='engine'?(Math.sin(t*2*Math.PI*70)+.4*Math.sin(t*2*Math.PI*143))*.18:kind==='alarm'?Math.sin(t*2*Math.PI*600-160*Math.cos(t*2*Math.PI))*.22:Math.sin(t*2*Math.PI*850)*Math.exp(-t*6)*.3;v.setInt16(44+i*2,Math.round(n*fade*32767),true);}return new Blob([buffer],{type:'audio/wav'});}
