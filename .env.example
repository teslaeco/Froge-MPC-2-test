# This server-only key encrypts paired Oracle credentials.
# Optional OpenAI API keys are configured through the owner's paired worker,
# stored privately on Oracle and never placed in frontend environment variables.
# Set a random 32-byte lowercase hex value through Sites runtime secrets.
# Keep the same value across deployments; rotation requires pairing again.
BLENDER_SETTINGS_KEY=
