# 2026-09-12 — generator camera fix verified; r6 real model preview delivered, quality NOT accepted

Source parent 8b7a6ab3b8083504a4545137535a474382498f80 adds geometry-based face framing, invalid-frame inspection rejection and a rooted closed-UV hair helper in the existing isolated bpy environment. Independent asset renderer now uses the same adaptive framing and works from an absolute script path. New module is included in the source dependency manifest. No installer ZIP, Oracle deployment, Site update, provider switch, GPU limit change or paid API call.

Confirmed real incident: anatomical eye object's origin [0,0,0], geometry z=1.56062..1.59778 in the edited GLB; old review targeted the origin, resulting in a blank face frame. New evaluated head target [0,.06132,1.54626], frame .44849. Real reimported GLB produced five valid MCP review views including the face. Native Blender regression checks origin invariance, intentional missing eye, invalid camera frame rejection and manifold/UV/root-fit hair. 52 Python tests passed, including dependency staging/rollback and invalid-frame inspection. Standalone render_asset_review.py also rendered actual r6 GLB successfully.

The user rejected prior r3 hair sheets. R4/r5 replaced sheets with separate spatial hair volumes, smoothed scalp hairline and anatomical outlines. R6 changes living eye/lids/brow/lashes coherently, lifts human mouth corner and adds exportable strand normal texture. Actual r6 GLB: 350301 triangles, 151 mesh objects, 8 embedded images. Rendered front/left/back with Cycles CPU 640x800/64 samples after reimport. SELF-ASSESSMENT: still inadequate likeness; hair remains rope-like, dental anatomy and garment construction are too simplified. Unseen back is inferred. Do not promote to accepted, print-ready or faithful reconstruction. See docs/reviews/direct-model-r6/r6-quality-report.json.

Saved standalone files successfully: FORGE-podglad-r6.png, r6-front.png, r6-left.png, r6-back.png, FORGE-model-poprawka-r6.blend and .glb. Existing original model and r3 remain saved. User preference: actual previews in chat and honest self-assessment, not another installation task. Continue reference-driven facial/dental/garment geometry improvements; current work does NOT resolve overall likeness quality. The broad generator still depends on authored/scripted geometry; more render samples alone cannot solve that.


Reproduction: load saved r3 .blend with auto-execution disabled, execute repair_model_r5.py (imports runtime/rooted_hair.py), then load r5 .blend and execute repair_model_r6.py. R4 is retained as an intermediate experiment. Update ROOT/OUT paths when running elsewhere. Render the exported r6 GLB with the saved r3 render_views.py, passing -- r6, or use oracle_connector/render_asset_review.py. Original r3 construction is saved in docs/reviews/direct-model-r3. No original reference pixels were pasted over the renders.
