# 2026-09-12 — R11 exactly 2,000,000 triangles; PBR and finer hair draft reviewed

Latest user prioritizes textures for skin/clothes/hair, calls thick strands unacceptable and requests 2000000 triangles. New uploaded reference is the same character artwork at a closer view. R11 starts from separately saved R10. Existing registered original face diffuse retained; 16 authored procedural UV maps at 2048x2048 added for skin, bone, ivory/charcoal cloth and chestnut hair (base colour, roughness, normal, plus facial microrelief). These maps add invented material detail, not recovered photographic information. FBX shader parity with all other applications is not guaranteed.

Each of the 104 long wave guides was replaced by three 8-sided thinner strands (312 total), keeping the wave centre paths. Initial radius .35 and very dark shader made coverage too sparse; final correct.py raises strand radius to .48 of original, warms chestnut colour, reduces orange in neck/body skin and balances ivory cloth contrast. R10 hairline geometry retained with updated hair shader. Global count restored with bounded per-corner-UV-preserving barycentric subdivision, which adds density rather than observed shape detail.

Final actual FBX reimport confirms exactly 2000000 triangles, 443 meshes, all 443 with UVs, no missing images. Export contains 20 used texture files including 16 new 2K maps. Colour face, neutral clay face, front, left and back rendered from final FBX at 640x800, Cycles CPU 24 samples. Visually checked face and all three body directions. Materials and strand thickness improved, but hairline remains too geometric, face/neck transition and dentition still need work, and hidden anatomy is inferred. No exact likeness or print-readiness claim; mesh is not certified watertight/intersection-free.

Deliverables: FORGE-model-r11-2000k.blend, .fbx, .glb; FORGE-r11-trzy-widoki.png; FORGE-r11-twarz.png. All five files saved successfully; each ordered result confirmed succeeded and local metadata applied. Reproduce: open R10, run materials_hair.py then correct.py, then render.py and compose.py. Source checkpoint f069a0bbf8066775b9f3cceffb10337aa55857c0. No Oracle/Site update, paid API calls, GPU quota changes or claim that these direct repairs fix all generator output.
